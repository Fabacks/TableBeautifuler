document.addEventListener("DOMContentLoaded", () => {
    // Initialiser TableBeautifuller pour le tableau d'exemple
    let table = new TableBeautifuller("demo-table");

    // On peut détruire le tableau
    // table.destroy();
});